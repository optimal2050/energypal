% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/order.R
\name{energypal_orders}
\alias{energypal_orders}
\title{Orderings available for a palette}
\usage{
energypal_orders(palette = NULL, file = NULL)
}
\arguments{
\item{palette}{Name of a built-in or registered palette. Defaults to the
\code{energypal.palette} option, or \code{"carriers"}.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.}
}
\value{
A data frame with one row per available ordering and columns
\code{order}, \code{kind} (\code{"declared"}, \code{"property"} or \code{"computed"}), \code{n}, \code{note},
\code{source} and \code{url}.
}
\description{
Lists the orderings that can be passed to \code{order} in \code{\link[=energypal]{energypal()}},
\code{\link[=energypal_table]{energypal_table()}} and the scale functions, together with the note and
source recorded for each.
}
\section{Presentation only}{

Orderings arrange legends and chart series. They are \strong{not} model inputs.
The sequences, and the numeric properties some of them derive from, are
indicative values chosen so that charts read sensibly; they are not validated
figures and must not be used in calculations. Every declared ordering carries
a note saying so, printed by this function.
}

\examples{
energypal_orders("carriers")

# the note travels with the ordering
energypal_orders("carriers")$note[1]
}
\seealso{
\code{\link[=energypal]{energypal()}}, \code{\link[=energypal_table]{energypal_table()}}
}
