% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{energypal_info}
\alias{energypal_info}
\title{Available energy palettes}
\usage{
energypal_info(family = NULL)
}
\arguments{
\item{family}{Optional family name; return only that family's members.}
}
\value{
A data frame with one row per palette and columns \code{name}, \code{title},
\code{source}, \code{family}, \code{measure}, \code{default}, \code{type} (\code{"discrete"} or
\code{"continuous"}), \code{n} (number of colours), \code{nested} (whether the palette has
a hierarchy), \code{breaks}, \code{unit} and \code{builtin}.

Every colour \code{n} counts is one the palette file declares itself. A palette
that \code{extends} another inherits its taxonomy — names, labels, aliases,
hierarchy, orderings — but never its colours.
}
\description{
Lists the palettes that can be passed to \code{\link[=energypal]{energypal()}}, \verb{palette =} arguments
and the scale functions: the ones shipped with the package plus any
registered in this session with \code{\link[=energypal_register]{energypal_register()}}.
}
\details{
This is a function rather than a stored data frame (unlike
\code{RColorBrewer::brewer.pal.info}) so that registered palettes appear in it and
so it cannot fall out of step with the files on disk.
}
\section{Families}{

An atlas publishes several quantities and styles each differently, so its
scales ship as separate palettes grouped by \code{family} and distinguished by
\code{measure} — \code{windatlas_speed}, \code{windatlas_density} and so on. They are not
interchangeable. One member of each family is its default, so the bare family
name (\code{"windatlas"}) resolves to the most common measure.
}

\examples{
energypal_info()

# what a family offers
energypal_info(family = "windatlas")

# just the continuous palettes
i <- energypal_info()
i[i$type == "continuous", c("name", "measure", "unit")]
}
\seealso{
\code{\link[=energypal]{energypal()}} to retrieve a palette, \code{\link[=energypal_register]{energypal_register()}} to add one.
}
