% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{energy_colors}
\alias{energy_colors}
\title{Smart Energy Source Mapping (Enhanced)}
\usage{
energy_colors(
  sources,
  palette_name = "carriers",
  match_method = "auto",
  unmapped_color = "#999999",
  case_sensitive = FALSE,
  ...
)
}
\arguments{
\item{sources}{Character vector of energy source names to map}

\item{palette_name}{Character string specifying the palette to use (default: "carriers")}

\item{match_method}{Character string specifying mapping method:
"exact", "canonical", "alias", "fuzzy", "regex", or "auto" (default: "auto")}

\item{unmapped_color}{Color to use for unmapped sources (default: "#999999")}

\item{case_sensitive}{Logical, whether matching should be case sensitive (default: FALSE)}

\item{max_distance}{Maximum normalized distance for fuzzy matching (default: 0.4)}

\item{regex_patterns}{List of regex patterns or "auto" to generate from palette names}

\item{warn}{Logical, emit warnings for ambiguous matches (default: TRUE)}
}
\value{
A named vector of colors corresponding to the input sources
}
\description{
Intelligently map energy source names to colors using a unified matching system
with exact matches, canonicalized matching, hierarchy aliases, fuzzy matching,
and regular expressions.
}
\examples{
# Basic mapping
sources <- c("Coal", "Natural Gas", "Solar", "Wind")
energy_colors(sources)

# Fuzzy matching for similar names
sources <- c("coal power", "nat gas", "solar pv", "wind power")
energy_colors(sources, match_method = "fuzzy")

# Using regex patterns
energy_colors(sources, match_method = "regex")

# Using specific methods only
energy_colors(sources, match_method = "exact")
}
