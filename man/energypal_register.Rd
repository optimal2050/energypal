% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{energypal_register}
\alias{energypal_register}
\title{Register a user palette for this session}
\usage{
energypal_register(name, file)
}
\arguments{
\item{name}{Name to register the palette under.}

\item{file}{Path to the palette YAML file.}
}
\value{
Invisibly, the previously registered path for \code{name}, or \code{NULL}.
}
\description{
Makes a palette file available by name, so it can be used anywhere a built-in
palette name is accepted. The registration lasts for the session only; to make
it permanent, call this from \code{.Rprofile} or pass \verb{file =} explicitly.
}
\examples{
p <- energypal_create(c(Coal = "#2C2C2C", Solar = "#FFA500"), name = "mine")
f <- tempfile(fileext = ".yml")
energypal_write(p, f)
energypal_register("mine", f)
energypal("mine")
}
\seealso{
\code{\link[=energypal_create]{energypal_create()}}, \code{\link[=energypal_write]{energypal_write()}}, \code{\link[=energypal_info]{energypal_info()}}
}
