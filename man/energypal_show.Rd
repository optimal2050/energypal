% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display.R
\name{energypal_show}
\alias{energypal_show}
\title{Plot energy palettes}
\usage{
energypal_show(
  palette = NULL,
  n = NULL,
  file = NULL,
  label_style = c("default", "id", "short", "long"),
  include_groups = FALSE,
  include_subtypes = FALSE,
  show_hex = TRUE
)
}
\arguments{
\item{palette}{Palette name, or a character vector of names to compare.
Defaults to the \code{energypal.palette} option, or \code{"carriers"}. Pass
\code{energypal_info()$name} to see everything at once.}

\item{n}{Number of colours to show from each palette. \code{NULL} shows all.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.
Only meaningful for a single palette.}

\item{label_style}{How to label entries: \code{"default"}, \code{"id"}, \code{"short"} or
\code{"long"}. See \code{\link[=energypal]{energypal()}}.}

\item{include_groups, include_subtypes}{Passed to \code{\link[=energypal]{energypal()}}; both \code{FALSE}
by default, matching the plotting palette.}

\item{show_hex}{Logical; show the hex code alongside the name in the
single-palette view (default \code{TRUE}).}
}
\value{
Invisibly, the palette(s) drawn: a named colour vector for a single
palette, or a named list of them.
}
\description{
Draws a palette so you can look at it. Given several palette names it draws
them as aligned rows instead, one row per palette and one column per entry,
which is the quickest way to see how two published colour schemes differ.
}
\examples{
energypal_show("carriers")

# Compare published schemes
energypal_show(c("carriers", "eia", "ipcc", "owid"))

# Everything the package ships
energypal_show(energypal_info()$name)
}
\seealso{
\code{\link[=energypal]{energypal()}} for the colours themselves, \code{\link[=energypal_info]{energypal_info()}} for
the list of palettes.
}
