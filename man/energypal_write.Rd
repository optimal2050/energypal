% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{energypal_write}
\alias{energypal_write}
\title{Write a palette to a YAML file}
\usage{
energypal_write(pal, file)
}
\arguments{
\item{pal}{A palette from \code{\link[=energypal_create]{energypal_create()}}, or any named colour vector.}

\item{file}{Path to write to.}
}
\value{
Invisibly, \code{file}.
}
\description{
Serialises a palette so it can be edited by hand and reloaded. Entries are
written in the simple \code{Name: "#RRGGBB"} form; any entry can then be expanded
in place into a map with \verb{_color}, \verb{_short}, \verb{_long} and \verb{_aliases} without
restructuring the rest of the file.
}
\examples{
p <- energypal_create(c(Coal = "#2C2C2C", Solar = "#FFA500"), name = "mine")
f <- tempfile(fileext = ".yml")
energypal_write(p, f)
cat(readLines(f), sep = "\n")
}
\seealso{
\code{\link[=energypal_create]{energypal_create()}}, \code{\link[=energypal_register]{energypal_register()}}
}
