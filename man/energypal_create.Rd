% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{energypal_create}
\alias{energypal_create}
\title{Build a palette from a named colour vector}
\usage{
energypal_create(x, name = "custom", title = NULL, extends = NULL)
}
\arguments{
\item{x}{Named character vector of hex colours, e.g. \code{c(Coal = "#2C2C2C")}.}

\item{name}{Palette name. Defaults to \code{"custom"}.}

\item{title}{Human-readable title. Defaults to \code{name}.}

\item{extends}{Optional name of a palette to inherit labels and aliases from.}
}
\value{
A palette specification list, with class \code{energypal_create}.
}
\description{
Turns a named character vector of hex colours into a palette object that can
be written to YAML with \code{\link[=energypal_write]{energypal_write()}}, registered with
\code{\link[=energypal_register]{energypal_register()}}, or used directly.
}
\details{
This is the quickest way in: start from a vector, save it, then edit the YAML
to add labels and aliases where they earn their keep.
}
\examples{
p <- energypal_create(c(Coal = "#2C2C2C", Solar = "#FFA500"), name = "mine")
p$meta$name
names(p$colors)
}
\seealso{
\code{\link[=energypal_write]{energypal_write()}}, \code{\link[=energypal_register]{energypal_register()}}
}
