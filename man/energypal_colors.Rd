% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{energypal_colors}
\alias{energypal_colors}
\alias{energypal_colours}
\title{Colours for a vector of data labels}
\usage{
energypal_colors(
  sources,
  palette = NULL,
  file = NULL,
  method = "auto",
  unmapped_color = "#999999",
  case_sensitive = FALSE,
  max_distance = 0.4,
  warn = TRUE,
  gradient = FALSE,
  spread = 0.1,
  along = c("lightness", "chroma")
)

energypal_colours(
  sources,
  palette = NULL,
  file = NULL,
  method = "auto",
  unmapped_color = "#999999",
  case_sensitive = FALSE,
  max_distance = 0.4,
  warn = TRUE,
  gradient = FALSE,
  spread = 0.1,
  along = c("lightness", "chroma")
)
}
\arguments{
\item{sources}{Character vector of labels from your data. Repeats are allowed;
the result has one row per distinct label.}

\item{palette}{Name of a built-in or registered palette. Defaults to the
\code{energypal.palette} option, or \code{"carriers"}.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.}

\item{method}{Stages to apply, in order. \code{"auto"} (default) uses all of them;
otherwise any of \code{"exact"}, \code{"canonical"}, \code{"alias"}, \code{"fuzzy"},
\code{"contains"}.}

\item{unmapped_color}{Colour for labels that no stage could resolve
(default \code{"#999999"}).}

\item{case_sensitive}{Logical; if \code{TRUE} the exact stage respects case
(default \code{FALSE}). Later stages are case-insensitive by construction.}

\item{max_distance}{Maximum normalised edit distance for the fuzzy stage,
between 0 and 1 (default \code{0.4}).}

\item{warn}{Logical; warn when a label matches entries of differing colours
(default \code{TRUE}).}

\item{gradient}{Logical; when several labels resolve to the \emph{same} carrier,
spread them into distinct shades of that carrier's colour instead of
drawing them identically (default \code{FALSE}). Grouping is by resolved carrier,
not by colour, so two carriers that happen to share a hex stay separate.}

\item{spread, along}{Passed to \code{\link[=energypal_gradient]{energypal_gradient()}} when \code{gradient = TRUE}.}
}
\value{
A character vector of colours, the same length and order as
\code{sources}, named by the input labels.
}
\description{
Maps the source labels in your data straight to colours, resolving them
against a palette with \code{\link[=energypal_match]{energypal_match()}}. Labels that cannot be
resolved get \code{unmapped_color}.
}
\examples{
energypal_colors(c("Coal", "Natural Gas", "Solar", "Wind"))

# messy labels resolve too
energypal_colors(c("coal power", "nat gas", "solar pv", "COAL1"))

# a different published palette, same labels
energypal_colors(c("Coal", "Natural Gas"), palette = "ipcc")

# several series per carrier: three coal shades, one solar
energypal_colors(c("COAL1", "COAL2", "COAL3", "SOLAR_NY"), gradient = TRUE)

# anything unresolved is visibly grey rather than silently wrong
energypal_colors(c("Coal", "Flux Capacitor"))
}
\seealso{
\code{\link[=energypal_match]{energypal_match()}} to see how each label was resolved,
\code{\link[=energypal_gradient]{energypal_gradient()}} for the shading itself, \code{\link[=energypal]{energypal()}} for the palette.
}
