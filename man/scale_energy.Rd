% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_energy}
\alias{scale_energy}
\alias{scale_fill_energy}
\alias{scale_colour_energy}
\alias{scale_color_energy}
\alias{scale_fill_energy_c}
\alias{scale_colour_energy_c}
\alias{scale_color_energy_c}
\alias{scale_fill_energy_b}
\alias{scale_colour_energy_b}
\alias{scale_color_energy_b}
\title{Energy colour scales for ggplot2}
\usage{
scale_fill_energy(
  palette = NULL,
  file = NULL,
  order = NULL,
  direction = 1,
  label_style = c("asis", "id", "short", "long", "default"),
  unmapped_color = "#999999",
  gradient = FALSE,
  guide = "legend",
  ...
)

scale_colour_energy(
  palette = NULL,
  file = NULL,
  order = NULL,
  direction = 1,
  label_style = c("asis", "id", "short", "long", "default"),
  unmapped_color = "#999999",
  gradient = FALSE,
  guide = "legend",
  ...
)

scale_color_energy(
  palette = NULL,
  file = NULL,
  order = NULL,
  direction = 1,
  label_style = c("asis", "id", "short", "long", "default"),
  unmapped_color = "#999999",
  gradient = FALSE,
  guide = "legend",
  ...
)

scale_fill_energy_c(
  palette = "windatlas",
  file = NULL,
  direction = 1,
  na.value = "grey50",
  ...
)

scale_colour_energy_c(
  palette = "windatlas",
  file = NULL,
  direction = 1,
  na.value = "grey50",
  ...
)

scale_color_energy_c(
  palette = "windatlas",
  file = NULL,
  direction = 1,
  na.value = "grey50",
  ...
)

scale_fill_energy_b(
  palette = "windatlas",
  file = NULL,
  breaks = NULL,
  direction = 1,
  na.value = "grey50",
  ...
)

scale_colour_energy_b(
  palette = "windatlas",
  file = NULL,
  breaks = NULL,
  direction = 1,
  na.value = "grey50",
  ...
)

scale_color_energy_b(
  palette = "windatlas",
  file = NULL,
  breaks = NULL,
  direction = 1,
  na.value = "grey50",
  ...
)
}
\arguments{
\item{palette}{Name of a built-in or registered palette. Defaults to the
\code{energypal.palette} option, or \code{"carriers"} — except for \verb{_c()} and \verb{_b()},
which need a continuous palette and default to \code{"windatlas"}.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.}

\item{order}{Legend order. \code{NULL} keeps the data's own order; otherwise a
declared ordering such as \code{"carbon_intensity"} or \code{"merit_order"}, a
computed one such as \code{"alpha"}, or an explicit vector. See
\code{\link[=energypal_orders]{energypal_orders()}}.

\strong{Presentation only} — orderings arrange the legend and are not model inputs.}

\item{direction}{\code{1} (default) or \code{-1} to reverse the order.}

\item{label_style}{How to label the legend: \code{"asis"} (default) keeps the
labels your data uses; \code{"id"} shows the canonical identifier; \code{"short"},
\code{"long"} and \code{"default"} use the palette's display labels.}

\item{unmapped_color}{Colour for labels the palette cannot resolve
(default \code{"#999999"}), so they are visibly unmapped rather than silently wrong.}

\item{gradient}{Logical; shade labels that resolve to the same carrier into
distinct tones of it (default \code{FALSE}). See \code{\link[=energypal_gradient]{energypal_gradient()}}.}

\item{guide, na.value, ...}{Passed to the underlying ggplot2 scale.}

\item{breaks}{For \verb{_b()}, the bin boundaries. \code{NULL} (default) uses the
palette's declared breaks.}
}
\value{
A ggplot2 scale, to be added to a plot with \code{+}.
}
\description{
Colour the series of a plot from an energy palette. The discrete scale
resolves whatever labels your data contains — \code{"Coal"}, \code{"nat gas"},
\code{"COAL1"} — through \code{\link[=energypal_match]{energypal_match()}}, so it works on real data without
preprocessing. The \verb{_c} and \verb{_b} variants take a continuous palette and give
a smooth gradient or binned steps.
}
\section{Choosing a variant}{

\describe{
\item{\code{scale_fill_energy()}}{discrete data: carriers, technologies, sectors.}
\item{\code{scale_fill_energy_c()}}{continuous data with a smooth ramp.}
\item{\code{scale_fill_energy_b()}}{continuous data in bins, using the palette's
own break points unless you override them. This is how resource maps are
conventionally drawn.}
}
Passing a continuous palette to the discrete scale, or the reverse, is an
error rather than a silent half-result.
}

\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)
  d <- subset(owid_energy_mix, year == max(owid_energy_mix$year))

  # the data says "Coal", "Natural Gas", "Oil" - no preprocessing needed
  ggplot(d, aes(country, percentage, fill = source)) +
    geom_col() +
    scale_fill_energy()

  # a published colour scheme
  ggplot(d, aes(country, percentage, fill = source)) +
    geom_col() +
    scale_fill_energy(palette = "ipcc")

  # order the stack by carbon intensity rather than alphabetically
  ggplot(d, aes(country, percentage, fill = source)) +
    geom_col() +
    scale_fill_energy(order = "carbon_intensity")
}
}
\seealso{
\code{\link[=energypal]{energypal()}} for the palette, \code{\link[=energypal_colors]{energypal_colors()}} for the colours
alone, \code{\link[=energypal_orders]{energypal_orders()}} for the available orderings.
}
