% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/energypal.R
\name{energypal_table}
\alias{energypal_table}
\title{Flatten a palette into a table}
\usage{
energypal_table(
  palette = NULL,
  file = NULL,
  include_groups = TRUE,
  order = NULL,
  direction = 1
)
}
\arguments{
\item{palette}{Name of a built-in or registered palette. Defaults to the
\code{energypal.palette} option, or \code{"carriers"}. See \code{\link[=energypal_info]{energypal_info()}}.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.}

\item{include_groups}{Logical; include the aggregate group rows (default \code{TRUE}).}

\item{order}{How to sequence the rows. \code{NULL} or \code{"spec"} keeps file order;
otherwise the name of an ordering declared in the palette, the name of a
numeric property (sorted descending), one of the computed orderings
\code{"alpha"}, \code{"hue"}, \code{"lightness"}, \code{"chroma"}, or an explicit character
vector of entry names. See \code{\link[=energypal_orders]{energypal_orders()}}.

\strong{Presentation only} — orderings arrange visuals and are not model inputs.}

\item{direction}{\code{1} (default) or \code{-1} to reverse the chosen order.}
}
\value{
A data frame with columns \code{name}, \code{level}, \code{parent}, \code{color}, \code{type},
\code{path}, \code{branch}, \code{label_short}, \code{label_long}, \code{aliases}, \code{label_default}
and \code{palette}, plus one column per numeric property the palette declares.
Property columns carry their \code{note}, \code{source}, \code{url} and \code{unit} as
attributes.
}
\description{
Returns one row per palette entry, with its colour, position in the
hierarchy, display labels and aliases. This is the form the matcher and the
scale functions work from, and the most convenient way to inspect a palette.
}
\examples{
tab <- energypal_table("carriers")
head(tab[, c("name", "level", "type", "color", "label_default")])

# Only the carrier-level rows
nrow(energypal_table("carriers", include_groups = FALSE))

# Declared properties become columns, and carry their caveat with them
tab <- energypal_table("carriers", include_groups = FALSE)
attr(tab$carbon_intensity, "note")
attr(tab$carbon_intensity, "unit")
}
\seealso{
\code{\link[=energypal]{energypal()}} for a colour vector, \code{\link[=energypal_orders]{energypal_orders()}} for the
available orderings, \code{\link[=energypal_info]{energypal_info()}} for the palette list.
}
