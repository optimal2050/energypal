% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{energypal_validate}
\alias{energypal_validate}
\title{Validate a palette specification}
\usage{
energypal_validate(x, name = NULL, strict = TRUE)
}
\arguments{
\item{x}{A palette specification (from \code{\link[=energypal_spec]{energypal_spec()}} or
\code{\link[=energypal_create]{energypal_create()}}), or a path to a palette file.}

\item{name}{Optional expected palette name, checked against \code{meta$name}.}

\item{strict}{Also require provenance. \code{TRUE} by default, since the usual
reason to call this directly is to check a palette meant for sharing.}
}
\value{
Invisibly \code{TRUE}. Throws an error describing every problem found.
}
\description{
Checks a palette for the problems that would otherwise surface as confusing
behaviour much later: a missing or misnamed \code{meta} block, colours that are not
hex, an \code{extends} target that does not exist, and duplicate entry names.
}
\details{
Used by the package's own test suite across every built-in palette, and by
\code{\link[=energypal_register]{energypal_register()}} before accepting a user file.
}
\section{Strict mode}{

\code{strict = TRUE} additionally requires provenance: a \code{meta$license} saying how
the colours were obtained, and, where that licence is a CC BY variant, the
\code{meta$attribution} it requires. This is the standard every palette in the
package must meet, and what a contributed palette is held to.

It is \emph{not} applied to palettes you register yourself. Your own colours need
no licence statement, and \code{\link[=energypal_register]{energypal_register()}} validates leniently so that
a project palette is a two-line file rather than a paperwork exercise.
}

\examples{
# your own colours: structure only
energypal_validate(energypal_create(c(Coal = "#2C2C2C")), strict = FALSE)

# what a shipped palette must satisfy
energypal_validate(energypal_spec("owid"), name = "owid")
}
