% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{energypal_match}
\alias{energypal_match}
\title{Match data labels to palette entries}
\usage{
energypal_match(
  sources,
  palette = NULL,
  file = NULL,
  method = "auto",
  case_sensitive = FALSE,
  max_distance = 0.4,
  warn = TRUE,
  include_full_palette = FALSE
)
}
\arguments{
\item{sources}{Character vector of labels from your data. Repeats are allowed;
the result has one row per distinct label.}

\item{palette}{Name of a built-in or registered palette. Defaults to the
\code{energypal.palette} option, or \code{"carriers"}.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.}

\item{method}{Stages to apply, in order. \code{"auto"} (default) uses all of them;
otherwise any of \code{"exact"}, \code{"canonical"}, \code{"alias"}, \code{"fuzzy"},
\code{"contains"}.}

\item{case_sensitive}{Logical; if \code{TRUE} the exact stage respects case
(default \code{FALSE}). Later stages are case-insensitive by construction.}

\item{max_distance}{Maximum normalised edit distance for the fuzzy stage,
between 0 and 1 (default \code{0.4}).}

\item{warn}{Logical; warn when a label matches entries of differing colours
(default \code{TRUE}).}

\item{include_full_palette}{Logical; append palette entries that nothing in
\code{sources} matched (default \code{FALSE}).}
}
\value{
A data frame with one row per distinct source and columns \code{original},
\code{matched} (entry name, or \code{NA}), \code{color}, \code{method} (which stage resolved it)
and \code{candidates} (\verb{;}-separated, when the match was ambiguous).
}
\description{
Resolves the source labels found in real datasets to the canonical entries of
a palette, and reports how each was resolved. Use this when you want to see or
audit the mapping; use \code{\link[=energypal_colors]{energypal_colors()}} when you just want the colours.
}
\details{
Matching proceeds in stages and stops at the first that succeeds for a given
label: exact, canonicalised (case, spacing, punctuation and digits removed),
declared alias, fuzzy (string distance), and finally containment, which
catches labels like \code{"Coal_Plant_2"}.

Aliases are declared in the palette file, so a project that supplies its own
palette can teach the matcher its own vocabulary without any code.
}
\examples{
energypal_match(c("Coal", "natural gas", "Solar PV", "COAL1"))

# every label the bundled dataset uses resolves
energypal_match(unique(owid_energy_mix$source))

# restrict to strict matching only
energypal_match(c("Coal", "nat gas"), method = c("exact", "canonical"))
}
\seealso{
\code{\link[=energypal_colors]{energypal_colors()}} for colours directly, \code{\link[=energypal]{energypal()}} for the palette.
}
