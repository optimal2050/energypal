% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{energypal_spec}
\alias{energypal_spec}
\title{Load a palette specification}
\usage{
energypal_spec(palette = NULL, file = NULL, force = FALSE)
}
\arguments{
\item{palette}{Name of a built-in or registered palette. Defaults to the
\code{energypal.palette} option, or \code{"carriers"}.}

\item{file}{Path to a palette file, taking precedence over \code{palette}.}

\item{force}{Logical; if \code{TRUE}, re-read even when cached.}
}
\value{
A list with \code{meta} and \code{colors} elements.
}
\description{
Reads a palette YAML file and returns its parsed contents, resolving
\code{meta.extends} against the palette it inherits from. The result is cached on
the file's path and modification time, so editing a palette during
development invalidates the entry on its own.
}
\examples{
spec <- energypal_spec("carriers")
names(spec)
spec$meta$title
}
\seealso{
\code{\link[=energypal]{energypal()}} for a colour vector, \code{\link[=energypal_table]{energypal_table()}} for a data frame.
}
